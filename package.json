{
  "name": "pi-auto-compact",
  "version": "1.0.0",
  "description": "Proactively compact Pi context at a configurable threshold and resume the current task. Standalone fork of HenryQW/pi-harness pi-auto-compact with dependencies removed (uses the current session model).",
  "keywords": [
    "pi-package",
    "pi",
    "compaction",
    "context"
  ],
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "license": "MIT",
  "files": [
    "LICENSE",
    "extensions",
    "README.md"
  ],
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "typescript": "^7.0.2"
  }
}
